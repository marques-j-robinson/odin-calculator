const calc = {
    add: (a, b) => a+b,
    subtract: (a, b) => a-b,
    multiply: (a, b) => a*b,
    divide: (a, b) => a/b,
}


const outputEl = document.querySelector('.output')
outputEl.innerText = 0
